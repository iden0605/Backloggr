# Plan

_Last updated: 2026-07-06 (tasks 18+19 done — Backlog → Library)_

## Current Phase

**v2 refinement phase (in progress).** Tasks 18+19 (Backlog → Library + browse/filter/sort) were designed and implemented 2026-07-06: a design round (4 AskUserQuestion decisions + an approved localhost mockup) locked the model — derived activity states with two manual marks, poster grid + `/library/:id` detail page, "Jump back in" idle hero — and the implementation landed the same session (compile/build clean, dev app launches). Remaining v2 tasks: 20–27, with 20 and 23 needing their own design rounds first.

**NEXT SESSION STARTS HERE:** (1) if the user hasn't confirmed the live click-through of the new Library/detail/Dashboard surfaces yet, get that confirmation (things to check: real cover art in the grid, a played game's detail page, wishlist tab, Playing badge while a game runs); (2) **this session's work is UNCOMMITTED** — check `git status`, the tasks 18+19 implementation + these context updates need a commit; (3) then the task 20 design round (merge Search + Discover into one find-a-game surface — mockup session before code, same pattern as task 18's round).

## Goals

- Build out backlog management (RAWG search, add/edit/status) end-to-end, with as little manual data entry as possible (auto-add on launch now; full Steam library import later)
- Layer in auto playtime tracking, dashboard analytics, AI recommendations, and clipping
- Ship a working Windows installer via CI/CD
- **v2:** evolve the backlog into a game library (browse + per-game stats, not a completion queue), unify search/discovery/AI recs into one coherent find-a-game experience, calm the dashboard down, polish UI/UX app-wide, harden clipping via real Windows testing, and close with a full software review

## Steps

| # | Task | Status | Notes |
|---|------|--------|-------|
| 1 | Scaffold Tauri v2 + React + TS + Vite project | Done | |
| 2 | Install frontend deps: tailwindcss v3, zustand, react-router-dom, lucide-react | Done | Pinned Tailwind to v3 (PostCSS), not v4 |
| 3 | Add Rust crates to `Cargo.toml` | Done | tauri-plugin-sql, sysinfo, rusqlite, reqwest, tokio, chrono, etc. |
| 4 | Implement `db.rs` schema init (games, sessions, clips, recommendations, settings) | Done | Verified via `get_backlog` command round-trip |
| 5 | Build sidebar layout + React Router views | Done | HashRouter used (safe under Tauri asset protocol) |
| 6 | Set up Zustand store skeleton | Done | `useAppStore.ts` |
| 7 | Verify dev build end-to-end | Done | `cargo check`, `tsc --noEmit`, `npm run build`, and `npm run tauri dev` all pass; app launched without panics |
| 8 | Implement RAWG search/backlog commands (`rawg.rs`, `commands.rs`) | Done | `search_rawg`, `add_game` (upsert), `update_game_status`, `delete_game`; RAWG key live and confirmed working |
| 9 | Build Search and Backlog React views | Done | Search (query + add) and Backlog (list, status dropdown, remove) built and wired; user click-tested full flow, confirmed working |
| 10 | Implement background process tracker (`tracker.rs`) + session commands | Done | `tracker.rs` polls `sysinfo` every 5s (tokio task spawned from `setup`), normalizes `.exe` suffix, opens/closes `sessions` rows, emits `session-started`/`session-ended` via `Emitter`. Added `set_game_exe_name` command + exe-name input in Backlog UI. Frontend listens for events in `App.tsx`, tracks `currentlyPlayingId` in Zustand. Click-tested live against real Steam/Dave the Diver process on Mac: session opened on launch, closed with correct `duration_seconds` on quit. Also verified DB survives an unclean `kill -9` of the app mid-session (WAL mode) — `PRAGMA integrity_check` returns `ok`, no data loss. No "currently playing" UI yet — deferred to Stage 4 (Dashboard). |
| 11 | Build Dashboard analytics commands + charts (recharts) | Done | `get_dashboard_stats(period)` (status counts, period-scoped total playtime, per-game playtime with cover art, last-7-days chart) + `get_currently_playing`; Dashboard.tsx renders a live "Currently Playing" banner (ticking elapsed timer), stat cards, recharts trend chart, and a "Games Played" list with thumbnails filterable by Day/Week/Month/All-Time. Recent-sessions list was dropped — not useful to the user, replaced by the per-game breakdown. |
| 11b | Tracker self-healing for unclean shutdowns | Done | `tracker::reconcile_dangling_sessions` runs at startup: re-adopts sessions for still-running games (preserving original `started_at`), or closes out dead ones using a new `last_seen_at` heartbeat column (flagged `ended_estimated`) instead of guessing 0 duration or inflating it to "now". Handles app crash, force-quit, and full OS restart/power-loss while a game was running. `get_currently_playing` reads live DB state on Dashboard mount instead of relying solely on events, avoiding a race with this reconciliation. |
| 11c | Auto-add unregistered games to the backlog on launch | Done | `tracker::detect_unregistered_games` scans running processes each poll for ones whose install path sits under a known storefront/launcher folder (`steamapps/common`, Epic Games, GOG Games, Battle.net, Riot Games) and isn't yet linked to a backlog entry. `auto_register_and_track` runs the guessed name through `humanize_name` (splits camelCase/letter-digit boundaries, e.g. `"BloonsTD6"` → `"Bloons TD 6"` — fixed after user found Bloons TD 6 got added with no cover art because the raw Steam folder name didn't match RAWG's listing) before searching RAWG for cover art/genre/platform, preferring an exact name match over just the first result (falls back to the humanized name alone on no match/API failure), upserts the `games` row (status `playing`), and opens its first session immediately — no manual "add to backlog" step needed. Emits a new `game-auto-added` event; Backlog.tsx listens and live-refreshes with a dismissible "X was automatically added" notice. |
| 11d | Fix `delete_game` foreign key constraint error | Done | `delete_game` only deleted the `games` row; with `foreign_keys = ON`, any game with `sessions`/`clips` rows referencing it (i.e. any game that had ever been tracked) failed to delete. Now wrapped in a transaction that deletes `sessions` and `clips` rows for that `game_id` first. User confirmed removing a previously-tracked game (Bloons TD 6) now works. |
| 11e | Backlog v2 — remove status dropdown, auto status-on-play | Done | User feedback: a manual status dropdown per game wasn't useful. Tracker now sets `games.status = 'playing'` automatically on session start (unless `completed`/`dropped`). New `get_playtime_totals` command feeds inline per-game playtime. Backlog.tsx groups games into Playing/Backlog/Wishlist/Completed/Dropped sections (Playing sorted by most-played), with contextual action buttons (`Mark Completed`/`Drop`/`Start Playing`/`Move to Wishlist`/`Move to Backlog`/`Play Again`) replacing the dropdown entirely; live-refreshes on `session-started`/`session-ended` in addition to the existing `game-auto-added`. |
| 12 | Deploy Cloudflare Worker proxy + implement `chat_recommend` command | Done | Stage 5 — expanded beyond the original scope into a chat-based recommendation feature (see Completed section) plus RAWG detail/expand metadata reused in Search |
| 12b | Stage 5.5 — chat UI/UX + AI quality fixes | Done | All 10 issues found via live testing fixed — see Completed section for detail. |
| 12c | AI recommendation quality v2 — candidate-pool narrowing + owned-game exclusion | Done | User follow-up request after 12b shipped, went beyond the original 12b scope. See Completed section. |
| 12d | Visual design system — Aurora Teal palette across the whole app | Done | User-requested full UI/UX pass with a locked color palette. See Completed section. |
| 13 | Implement ffmpeg rolling buffer + clip save (`clipper.rs`) + hotkey | Done | Stage 6 — see Completed section for the final continuous-buffer architecture after live-testing iteration |
| 13b | Clip UX polish round | Done | Four rounds total (see Completed). Closed 2026-07-04: NSPanel conversion live-confirmed fixing the overlay-over-fullscreen bug; "no audio" root-caused to dev-machine audio routing (not code) + mic-loopback guard added. macOS game-audio re-test (needs the user's one-time Multi-Output Device setup) left optional — dev-only platform. Windows end-to-end verification lives with task 16 where the WASAPI work happens. |
| 14 | System tray, background mode, launch-on-startup | Done | Stage 7 — implemented and closed 2026-07-04 (`tray.rs`, close-to-tray via `on_window_event`, `tauri-plugin-autostart` + Settings "General" toggle, `--hidden` autostart launches). User live-tested on macOS: tray menu Open/Quit, left-click opens, close-to-tray with tracking/hotkey still active, autostart toggle, quit — all confirmed working. `windows-check` CI passed `cargo check` on `windows-latest` for the committed code (registry autostart path, tray-icon feature). Real Windows hardware verification (visual/behavioral, not just compile) still lives with task 16. |
| 15 | Settings screen + polish (empty states, onboarding, error boundaries) | Done | Stage 8 — Settings screen (General/Clips sections) was already built pre-session. Added: shared `ErrorBoundary` per-route (`Shell.tsx`), Backlog empty-state CTA, Dashboard first-run onboarding card, Search pre-search hint + "No games found" empty state (Recommendations already had empty-state copy from task 12b, needed nothing new). `tsc --noEmit` and build clean throughout; ErrorBoundary/tab navigation live-verified via `npm run tauri dev` earlier in the session. Closed without a final visual pass on the empty-state cards themselves — informal, low-risk UI, can be caught in the 15b polish pass if anything looks off. |
| 15b | Stage 8.5 — full redesign: "Backdrop" layout + "Iron & Chalk" palette | Done | Scope grew from a polish pass into a full redesign (user request 2026-07-04: "completely redesign the feel and look... unique, captivating"). Direction chosen via three rounds of localhost HTML mockups (same pattern as 12d): round 1 (5 recolors of the current layout) rejected as "generic/AI-generated"; round 2 explored 4 quiet material palettes × 4 genuinely different layouts; round 3 explored 4 variations of the winning "Shelf" concept + a 6-palette lab. **Winner: "The Backdrop" layout** (top floating nav over a blurred hero of the currently-playing game's cover art, sharp poster + title + timer at hero base, cover-art-forward shelves below; hero falls back to a backlog pick when idle) **in "Iron & Chalk" palette** (near-black warm neutral `#151414`, chalk text `#EDE8E0`, rust `#B96A55` as a scarce "live marker" — timers/live dots/active nav only; cover art is the loudest thing on screen). Replaces Aurora Teal + sidebar shell across every page incl. overlay toast, empty states, error boundary. **Implementation landed 2026-07-04**: new palette + Archivo fonts in `tailwind.config.js`/`index.css`/`index.html`; `TopNav.tsx` (floating nav, live now-playing indicator, solid-on-scroll) replaced `Sidebar.tsx`; Shell gives Dashboard full-bleed, chat a viewport-height column, others a centered `max-w-5xl` column; Dashboard rebuilt around the hero (playing → up-next → jump-back-in → first-run states) + editorial stat strip + horizontal games-played shelf; all pages restyled (primary buttons are chalk, not rust; nav label "Recommendations" → "Discover"). `tsc`/`npm run build` clean. **Closed 2026-07-04**: user reviewed all 12 pages live via screenshots; a same-day polish round (shared EmptyState, control-contrast pass, first-run Dashboard rebuild, `.kbd` hotkey chips, chat/clips/settings refinements, Iron & Chalk tabbed_out.png) was applied and approved. |
| 16 | GitHub Actions release workflow (`release.yml`) + ffmpeg sidecar + WASAPI loopback | Done (code) | Stage 9 — implemented 2026-07-05, see Completed. `release.yml` builds `.msi`/`.nsis`/`.dmg` on `v*` tags via `tauri-apps/tauri-action` as a DRAFT release; Windows leg stages a pinned gyan.dev ffmpeg 8.0.1 essentials build as the `externalBin` sidecar (`tauri.windows.conf.json`); `clipper.rs::ffmpeg_path()` resolves the sidecar next to the exe with PATH fallback (dev/macOS); new `loopback.rs` captures the default output device via cpal/WASAPI loopback and pipes raw PCM into the capture ffmpeg's stdin — zero-setup game audio, dshow loopback devices demoted to fallback. `windows-check` CI green. **Still pending real-world validation**: first actual tag run of release.yml, and live Windows hardware testing (see Open Questions). macOS stays loopback-device-based (dev-only platform); ScreenCaptureKit remains the answer if macOS ever ships for real. |
| 17 | Steam library connect to auto-import the full owned-games list | Done | Stage 10 — implemented and live-verified 2026-07-06 (user fetched their real profile and confirmed the import works). New `steam.rs` (ResolveVanityURL + GetOwnedGames), two-step fetch→review→import flow (Settings "Steam" section + selection modal with progress bar), RAWG enrichment in concurrent chunks, `games.steam_appid` dedupe column. Key is compile-time-injected via `option_env!("STEAM_API_KEY")`: GitHub repo secret (set via `gh secret set`, covers release builds) + gitignored `src-tauri/.cargo/config.toml` for local dev (currently holds the placeholder — paste the key back in to test locally). |
| 18 | Backlog → Library rethink (user points 1, 3) | Done | Stage 11 — designed + implemented 2026-07-06 (see Completed). Derived activity model, Library grid + `/library/:id` detail page, "Jump back in" idle hero. Live click-through by the user pending. |
| 19 | Library browsing: search, filter, sort (user point 6) | Done | Stage 11 — landed together with task 18 (the toolbar is integral to the grid design): in-library search box, derived-activity filter chips, Library/Wishlist tabs, sort (last played / most played / name / added), all client-side over one `get_library` read. Genre/platform filters deliberately not included in v1 of the toolbar — add later if the user asks. |
| 20 | Merge Search + Discover into one coherent surface (user point 7) | Todo | Stage 12 — RAWG title search and AI discovery shouldn't be two separate tabs; design one "find a game" surface that serves both "I know what I want" (direct search) and "help me find something" (AI). Needs a design round — how do search results, For You, and chat coexist without clutter? Nav shrinks by one tab. |
| 21 | Unified AI recommendations (user point 8) | Todo | Stage 12 — combine the activity-based "For You" recs and chat recs into one experience: the 6-8 static For You cards waste space and feel limiting — add "load more", and make results conversational (start chatting *about* a recommendation set, refine it in place). Likely becomes the AI half of task 20's merged surface. Worker `/suggest` + `/chat` endpoints may merge too — keep the "model proposes, code decides" pattern. |
| 22 | Chat UX streamlining (user point 9) | Todo | Stage 12 — smoother chatbot feel: streaming-style response reveal, better loading states, message animations, input ergonomics. Audit against the ChatGPT/Claude bar the user originally asked for. |
| 23 | Dashboard layout + widget usefulness pass (user point 4) | Todo | Stage 13 — homepage is number-bombarding; make widgets calmer and more legible, especially the 7-day chart (rethink what it shows and how). Fewer, more meaningful numbers; more glanceable presentation. Mockup round with the user before rebuilding. |
| 24 | Global UI/UX polish: animations, selection, focus (user point 5) | Todo | Stage 13 — sweep for: text-selection highlighting on buttons/labels (`select-none` where appropriate), missing hover/press/focus states, transition/animation consistency, highlight colours. App-wide audit, page by page. |
| 25 | Clipping system: Windows live-test bug fixes + UX (user points 2, 10a) | Todo | Stage 14 — user will live-test capture on real Windows hardware and report bugs (gdigrab, WASAPI loopback pacing, dshow mic, window-scoping) — fix what surfaces. Plus general clip UX friendliness: better save feedback, clip management (rename? trim? open folder), clearer Settings copy. |
| 26 | Configurable clip hotkey (user point 10) | Todo | Stage 14 — replace the hardcoded `Alt+F9` (`CLIP_HOTKEY` const in lib.rs) with a user-set hotkey in Settings: either a curated dropdown or a record-a-combo capture field. Needs re-registration via `tauri-plugin-global-shortcut` at runtime + persistence in `settings` table + all UI copy that says Alt+F9 (`.kbd` chips) reading the live value. |
| 27 | Full software review (user point 11) | Todo | Stage 15 — capstone: codebase architecture review (module boundaries, commands.rs size, frontend state), functional review of every element (tracking, clipping, AI recs, dashboard, library, Steam import, tray/autostart), and a UI/UX review pass. Produces a findings list that may spawn final fix tasks. Run after 18-26 land so it reviews the finished v2, not a moving target. |

## Completed

- Full Stage 1 scaffold: Tauri v2 + React shell, SQLite schema init, sidebar navigation, Zustand store — build and dev-run verified clean.
- Full Stage 2: RAWG search + backlog management (search, add, status change, remove) — user click-tested end-to-end.
- Full Stage 3: background playtime tracker — session start/end via `sysinfo` polling, exe-name linking UI, live events to frontend. Click-tested against a real running game; DB confirmed crash-safe (WAL mode).
- Full Stage 4: Dashboard analytics — `get_dashboard_stats` Rust command aggregates playtime/session data; Dashboard view shows stat cards, a recharts bar chart of the last 7 days' playtime, top-5 most-played games, and 10 most recent sessions. Verified via `cargo check`, `tsc --noEmit`, `npm run build`, and a live `npm run tauri dev` run (no panics, HMR round-tripped cleanly).
- Dashboard v2 + tracker self-healing (user feedback after using Stage 4 build): removed the recent-sessions list in favor of a period-filterable (Day/Week/Month/All-Time) "Games Played" list with cover art; added a live "Currently Playing" banner with a real-time ticking elapsed timer (fixes: previously required a tab switch to see session start/end reflected); added `tracker::reconcile_dangling_sessions` + `last_seen_at` heartbeat so a crash, force-quit, or full PC restart while a game was running no longer produces a permanently-open phantom session or double-counted playtime. Verified via `cargo check` and `tsc --noEmit`; user testing live with Dave the Diver in progress.
- Auto-add-to-backlog (user feedback: didn't want to manually add every owned game): tracker now detects games launched from Steam/Epic/GOG/Battle.net/Riot install paths that aren't already in the backlog, best-effort enriches via RAWG, and adds + starts tracking them with zero manual steps. Full Steam-account library import (sign-in based, covers never-launched games too) intentionally deferred to Stage 10 — see Steps table task 17.
- Auto-add name matching + delete bug fixes (found via live testing with Bloons TD 6): RAWG search now humanizes storefront folder names before matching (fixed missing cover art); `delete_game` now cleans up dependent `sessions`/`clips` rows in a transaction (fixed a foreign key constraint error that blocked deleting any previously-tracked game). Both confirmed fixed by the user live.
- Backlog v2 (user feedback: the status dropdown wasn't useful/discoverable): status now auto-updates to `playing` when the tracker detects a launch; Backlog UI reorganized into status-grouped sections with contextual action buttons and inline playtime instead of one flat list + dropdown per row. Verified via `cargo check` and `tsc --noEmit`; live user testing pending.
- Full Stage 5 + chat recommendations (expanded scope, user request): deployed `proxy/src/index.ts`, a Cloudflare Worker proxying to **Groq** (not Ollama — free tier, no self-hosting) with a system prompt constrained to strict `{"type":"clarify"|"search", ...}` JSON. New Rust `chat_recommend` command forwards chat turns, and runs `rawg::search_games` itself once the model signals it's ready — RAWG key never touches the worker. `Recommendations.tsx` is now a chat UI (in-memory history only, per user's explicit choice) that can ask one clarifying question before showing results. Also added `rawg::get_game_details` (RAWG's `/games/{id}` for description/Metacritic/developer/publisher/website) and a shared `GameCard` component (compact grid card + click-to-expand detail panel) used by both `Recommendations` and `Search`, replacing Search's old bare card markup. User deployed the worker themselves via `wrangler deploy` + a registered `*.workers.dev` subdomain; verified via `cargo check`, `tsc --noEmit`, `npm run build`, and a clean `npm run tauri dev` launch (no panics).
- Stage 5 iteration round (live user testing found real quality/UX issues, fixed same session):
  - Chat quality: RAWG's plain keyword search surfaced a wall of same-title reskins (e.g. "Farming Simulator 2013/15/17/19/22" for "games like Stardew Valley") — fixed by having Groq name specific distinct titles it knows (`titles: string[]`) and resolving each individually via new `rawg::resolve_titles`, capped at 8 results.
  - Worker URL hardcoded as a Rust const instead of a user-facing Settings field (user explicitly wanted it hidden, same pattern as the RAWG key) — added then removed `get_setting`/`set_setting` commands and the Settings UI field in the same session.
  - Added a Dashboard-activity-based recommendation source: `get_dashboard_recommendations` picks the user's top-played genre + 3 favorite games, calls the worker's new `/suggest` endpoint, and caches the result in a new `dashboard_recommendations_cache` table (single row) — only regenerates on a 5-hour timer, backlog-size change, top-genre change, or a 2-hour jump in that genre's playtime, so it isn't an AI call on every page load. Later moved from a Dashboard widget into a "For You" tab on the Recommendations page (user request) — Dashboard no longer shows AI recommendations.
  - Fixed the chat always searching immediately even on a first, vague message ("give me a new game") — the model wasn't reliably self-judging "should I clarify or search" from prose alone. Rewired to a deterministic contract: the frontend explicitly sends `awaitingAnswer` (true = this message answers a pending clarifying question, false = new ask), and the worker picks between two separate system prompts based on that flag rather than asking the model to decide. Conversation history is no longer truncated between asks (an earlier fix had reset it after every results turn to avoid unrelated-topic bleed, but the user wanted continued/deepening conversations instead — the explicit flag makes the reset unnecessary).
  - Fixed the clarifying question always asking the same generic "what aspect appeals to you" regardless of prompt specificity (e.g. still asked that for "MMORPG with classes and races", which already answered several axes) — rewrote `CLARIFY_SYSTEM_PROMPT` to have the model reason about what the request does NOT yet specify (setting/theme, tone, pacing, difficulty, social angle, art style, specific mechanic) and pick a non-repeating axis, instead of defaulting to one canned question shape.
  - Added multi-select clarifying questions (`multiSelect` field end-to-end: worker → `chat_recommend` → frontend) with a checkbox-style option UI + "Continue" button, for questions like "what do you like about X" where more than one answer applies; single-choice questions still send immediately on click.
  - UI fixes: results now show an explicit "Here are some games based on..." lead-in line above the grid (both Chat and For You tabs); `GameCard`'s expanded panel puts genre/Metacritic on one row and platforms on a separate row (was cramped on one line); `GameCard`'s compact layout is a flex column with the "Add to Backlog" button pinned to the bottom via `mt-auto` (was misaligned across a grid row depending on genre/platform line count); platform icons collapse to at most 4 deduped category icons (Windows/Mac/Console/Mobile) instead of one icon per individual RAWG platform entry (was showing e.g. 4+ repeated controller icons for PS4/PS5/Xbox One/Xbox Series).
  - Remaining known bugs/quality issues (RAWG link click not opening, chat bubble sizing, no smooth-scroll, input disabling while AI loading, generic placeholder copy) deliberately deferred to Stage 5.5 — see Steps table task 12b.
- Stage 5.5 — chat UI/UX + AI quality fixes (task 12b, all 10 items): RAWG link now opens via `@tauri-apps/plugin-opener`'s `openUrl()` instead of a dead `<a target="_blank">`; chat bubbles resize to content with proper tail styling; transcript auto-scrolls on every turn; the composer stays typable while the AI is "thinking" (only Send disables) and grew into an auto-expanding multi-line textarea (Enter sends, Shift+Enter newlines); chat state (`chatTurns`, `chatQuestionsAsked`) moved into the Zustand store so it survives route navigation and For You/Chat tab switches; copy pass across Recommendations' placeholders/empty states; worker prompts gained a `TOPIC_FOCUS_NOTE` so the model resolves the *current* ask instead of blending in earlier unrelated asks from the same never-truncated history; the fixed one-question-then-search flow became a variable-length narrowing loop (see 12c for the final code-side-decision version).
- AI recommendation quality v2 (task 12c, user follow-up after 12b): reworked the worker's `/chat` endpoint around a single `narrowSystemPrompt` — the model returns a real candidate pool (actual named titles + a short per-game reason) plus its best pool-splitting question every round, but the decision to show results vs. ask again now lives in code (`candidates.length <= 8` or a 4-round cap), never in the model's own judgment, matching the project's established "model proposes, code decides" pattern. Added an owned-games exclusion list (`owned_game_names` in Rust, sent as `excluded` to both `/chat` and `/suggest`) so nothing already in the player's library gets recommended — filtered in the prompt and again in code as a backstop. Added per-game "why this fits" reasons via new `rawg::resolve_titles_with_reasons` and a `RecommendedGame` type, shown on result cards. Dropped the `awaitingAnswer` flag entirely in favor of a `questionsAsked` counter now that the flow is pool-size-driven rather than a fixed two-step exchange.
- Stage 6 — ffmpeg rolling-buffer clip capture + hotkey (task 13), through several live-testing iteration rounds to its final architecture: `clipper.rs` runs ONE continuous ffmpeg capture per game session (`ensure_capture`, called from `tracker.rs` at session open/close — never stopped/restarted on focus changes) writing MPEG-TS segments (`-segment_wrap`, 10s segments, 3-min ring buffer in `<appdata>/clip_buffer/`) plus an AAC mic track. `Alt+F9` (hardcoded global shortcut, same pattern as the RAWG key/worker URL) calls `save_clip`, which re-encodes (not stream-copy — needed for exact-length output seeking and the black-out filter below) the trailing N seconds into `<appdata>/clips/`, attached to whichever game has an open session.
  - **Continuous-buffer design, not pause/swap**: two earlier architectures (dropping tainted segments at save time; live-killing and swapping ffmpeg for a black-frame generator on every focus change) both proved buggy under real tab-out testing — swap-kills produced 0-byte segments, broke duration math (clips came out 24-40s instead of the requested 30), and mask timing drifted enough to invert which span got blacked out. The fix, confirmed by the user to match how Medal/ShadowPlay/Outplayed work: never stop the buffer: a 1-second focus sampler (`is_frontmost`, `lsappinfo` on macOS — NOT `osascript`/System Events, which silently fails without an Automation permission dev binaries never get prompted for) just logs focus state; `save_clip` masks the logged unfocused ranges to solid black in the OUTPUT via an ffmpeg `drawbox...enable=` filter, anchored on the selected segments' own precise file timestamps (an earlier version anchored on `now() minus a summed-duration estimate`, which drifted and blacked the wrong span). Only applies when capture isn't window-scoped — Windows' `gdigrab title=` capture structurally can't contain non-game content regardless of focus, so real gameplay records straight through alt-tabs there with no masking needed at all (this is genuinely superior to the Mac dev-only fallback, matching the "GPU/window hook" tier of capture the user's research called out for tools like Medal).
  - **Self-healing against orphaned capture processes**: dev rebuilds (and, on shipped installs, crashes/force-kills) can kill the app without running its exit cleanup, orphaning the capture ffmpeg — which then keeps recording indefinitely into a buffer a fresh app instance would read from. Each spawn writes its PID to `capture.pid`; `reap_orphan_capture` (called at app startup and before every fresh capture spawn) kills a leftover process found there after verifying it's still actually an ffmpeg (PIDs get reused). `tauri-plugin-single-instance` also prevents two copies of the app running at once, which was independently capable of causing the same symptom.
  - **Mic audio**: added end-to-end (macOS `avfoundation` combined video:audio input; Windows auto-detects the default `dshow` device) and captured continuously through tab-outs — only the video gets masked, never the audio track. Falls back to video-only after 2 consecutive capture-spawn failures (`CaptureSlot::respawn_strikes`) rather than crash-looping if the audio input is the problem. Not yet exposed as a user toggle — see task 13b.
  - Exact clip length: `recent_segments` returns the real wall-clock span the selected segments cover (from file birth/modified times, not an assumed 10s-per-file), and `save_clip` uses `-ss`/`-t` on the re-encode to cut precisely the requested trailing window.
  - Native notifications: the `tauri-plugin-notification` path silently does nothing for unbundled `cargo run` dev binaries on macOS (permission reads Granted, `.show()` returns Ok, nothing appears) — worked around by shelling to `osascript -e 'display notification...'` on macOS specifically, Windows/packaged apps use the plugin normally. **Still not confirmed working live on macOS as of the last test** — see task 13b, this needs further debugging, the `osascript` workaround may not be sufficient on its own.
  - Ffmpeg is still a bare PATH lookup — bundling it as a Tauri sidecar binary remains deferred to the release-workflow stage (16).
  - `Clips.tsx` shows a thumbnail grid with a click-to-play modal (`convertFileSrc`, asset protocol scoped to `$APPDATA/clips/*`). Settings has a working clip-length field (`get_clip_seconds`/`set_clip_seconds`, backed by the `settings` table).
  - Live-tested extensively on macOS with real Dave the Diver sessions through many rounds of tab-out/tab-in; **Windows path (`gdigrab`, `dshow`, window-title resolution via the `windows` crate) is unverified beyond `cargo check` and the `windows-check` CI job** — no access to real Windows hardware in this environment. See task 13b for the open polish items.
- Visual design system (task 12d, user-requested UI/UX pass): compared several dark-palette directions with the user via disposable local HTML mockups (served over `localhost`, iterated live) before landing on **Aurora Teal** (`bg #0B1215`, `accent #2DD4BF`, full palette in `tailwind.config.js` as semantic color names). Added a Fraunces/Instrument Sans/JetBrains Mono font pairing. Applied across Sidebar/Shell, GameCard, Dashboard, Backlog, Search, Clips, Settings, and fully redesigned the Recommendations chat UI in a ChatGPT/Claude-inspired style (plain-text assistant turns with a small avatar instead of bubbles, pinned composer via real flexbox rather than fragile `sticky` positioning). Fixed two bugs found via user screenshots during the pass: the expand-detail modal rendered off-center because a hover `transform` on the card created a new containing block for its `fixed` overlay (fixed via `createPortal` to `document.body`), and the composer had inflated padding from a conflicting inline-height/`rows` interaction (fixed by switching to content-driven sizing). Also removed an italic display-serif treatment from page titles and the sidebar logo after explicit user pushback ("looks bad").

- Task 13b round 1 (gradient mask, saving-state toast, mic toggle) + round 2 (live-test bug fixes and the in-game overlay). The user's first live test found five real bugs, all root-caused and fixed the same session:
  - **Empty/unplayable 262-byte clips**: `-segment_wrap` recycles segment files in place and recycled files keep their ORIGINAL creation time (macOS), so once the ring wrapped (~3 min in) the birth→mtime duration math inflated wildly, pushing the output `-ss` seek past the real footage — ffmpeg exits 0 having written a zero-frame MP4. `recent_segments` now derives each segment's span from mtime deltas between consecutive files (never birth times), stops selection at footage holes (delta ≫ segment length, i.e. capture died/respawned), and `save_clip` hard-fails (deleting the junk file) if the finished clip probes as zero-duration instead of cataloging it.
  - **No audio in clips**: the hardcoded avfoundation device string `"2:1"` had gone stale — audio index 1 on the dev machine is now BlackHole (silent virtual device), not the mic, because connected devices (JBL headset, iPhone) reorder the list. Devices are now resolved at every spawn: screen index parsed by name from `-list_devices` output, audio uses avfoundation's `default` keyword (follows the system-default input). Note game/system audio was never in scope on mac (needs loopback routing); mic-only is the current design, Windows WASAPI loopback is a future packaging-stage upgrade.
  - **10s saves**: the `geq` per-pixel gradient was the cost (per-frame expression eval at 2560×1664). The mask is now a pre-rendered PNG (`src-tauri/resources/tabbed_out.png`, vignette + "Tabbed out / Back in a moment" text baked in — generated with PIL, committed as an asset, bundled via tauri.conf `bundle.resources`) composited with `scale2ref`+`overlay=enable='...'`. 30s Retina save ≈ 4.6s total (verified with a real ffmpeg run); also kills the drawtext dependency entirely (`drawtext_supported()` probe removed). Falls back to flat-black `drawbox` if the resource is missing.
  - **Clips include the whole screen in windowed mode**: macOS clips are now cropped to the game window at save time — `game_window_rect_points()` (CGWindowListCopyWindowInfo via new macOS-only `core-graphics`/`core-foundation` deps; bounds need no TCC permission) finds the largest window of the game's pids, `crop_filter_for_game_window()` maps points→capture pixels using the probed segment resolution vs. display point size (avfoundation captures native panel pixels — neither point size nor backing size, so the scale MUST come from the actual footage). Fullscreen/whole-display windows skip the crop; off-main-display windows fall back to uncropped. Window position is sampled at save time, so a window dragged mid-clip shows offset early footage — accepted tradeoff. (Windows needs none of this: `gdigrab title=` is true window capture at the source.)
  - **Mic toggle didn't apply live**: the setting was only read at capture spawn. `set_mic_enabled` now kills the live capture with a `restart_requested` flag on `CaptureSlot`; the tracker watchdog respawns it (≤5s) reading the new setting, treating it as a fresh start (buffer wipe — segments with a different stream layout can't concat) and NOT counting an audio-failure strike. Also fixed the toggle knob CSS (missing `left` anchor made it overflow the switch).
  - **In-game overlay toast replaces OS notifications entirely** (user request — the Medal/ShadowPlay approach; macOS suppresses notification banners whenever a fullscreen app is frontmost, which the log proved was why "granted + osascript success" still showed nothing in-game): new `overlay.rs` creates a small (360×72) transparent, always-on-top, click-through, non-focusable window at app setup (main-thread requirement), hidden until `overlay::toast()` shows it pinned top-right of the primary display. It renders the React bundle at `#/overlay` (new `OverlayToast.tsx` route outside the Shell, body forced transparent), receives `overlay-toast` events, updates in place (saving spinner → saved/failed), and Rust auto-hides it (4s terminal / 30s stuck-save fallback, generation-counter guarded). macOS needed raw objc (`setLevel: 1000`, `setCollectionBehavior: CanJoinAllSpaces|FullScreenAuxiliary` — new `objc` dep) to float over fullscreen Spaces, plus the `macos-private-api` tauri feature + `macOSPrivateApi` conf flag for window transparency. Capability file now lists `clip-overlay` alongside `main` (a webview without a capability can't even `listen`). Known limit shared with all non-injecting overlays: Windows exclusive-fullscreen games won't show it (borderless is fine). The redundant in-app `ClipToast` in Clips.tsx was removed; `notify()` and the notification-permission request are gone (plugin still registered but unused).

- Task 13b round 3 (game/system audio + overlay-over-fullscreen attempt + tabbed-out redesign, same session as rounds 1-2 after the user's second live test):
  - **Game/system audio via loopback device**: capture now takes up to two audio inputs — the mic (macOS avfoundation `default`; Windows dshow device that *names itself* a mic via `dshow_mic_device`, since dshow has no default-device concept) and a system-audio loopback device when installed (`is_loopback_device_name`: BlackHole/Soundflower/Loopback on macOS, Stereo Mix/virtual-audio-capturer on Windows). Two tracks get mixed with `amix` in `spawn_ffmpeg`, each upconverted to 48kHz stereo BEFORE mixing — amix otherwise negotiates the lowest common format, and the user's 16kHz-mono headset mic audibly degraded the game track in the live pipeline test. Mapping is now always explicit (`-map`) instead of ffmpeg's default best-stream selection; live inputs get `-thread_queue_size 512`. Verified against real devices (default mic + BlackHole → 48kHz stereo AAC). **Loopback only carries what's routed into it** — macOS needs the user's one-time Multi-Output Device setup (Audio MIDI Setup → Multi-Output → speakers + BlackHole → select as output), documented to the user; on Windows the whole loopback-device path becomes a fallback once native WASAPI loopback lands (task 16 decision — that's how Medal/ShadowPlay ship zero-setup game audio).
  - **Overlay-over-fullscreen attempt (did NOT fix it)**: `elevate_above_fullscreen` now re-runs on every toast via `run_on_main_thread` (tao reasserts its own window level during `show()`), sets level to `CGShieldingWindowLevel()+1`, CanJoinAllSpaces|FullScreenAuxiliary, and `orderFrontRegardless`. User's re-test with a fullscreen Steam game: toast still hidden (appears on tab-out). Bug stands, deferred — leads in Open Questions.
  - **Tabbed-out card redesign** (user: "more professional"): regenerated `resources/tabbed_out.png` — SF Pro Semibold (PIL variable-font variation) letter-spaced uppercase "TABBED OUT", 44px teal accent bar (#2DD4BF), "Gameplay resumes in a moment" sub-line, softer bicubic-upscaled vignette.

- Task 13b close-out (2026-07-04): **overlay-over-fullscreen FIXED** — the NSPanel conversion (`convert_to_panel`: class-swap to a non-activating panel with `hidesOnDeactivate:NO`, + `Stationary` collection behavior + per-toast diagnostics readback) was live-confirmed by the user with a fullscreen Steam game. The same session's "still no audio in clips" report was diagnosed (via ffprobe/volumedetect on the actual clip: one track, flat -91 dB) as three stacked dev-machine conditions: BlackHole had made itself the system-default INPUT device (so the mic's `default` keyword recorded a silent loopback), the app's mic toggle was off, and system output wasn't routed through a Multi-Output Device (so the loopback side was silent too). Code hardening landed from it: macOS mic selection now checks the default input's name via `system_profiler SPAudioDataType` (`macos_default_input_name`) and, when it's a loopback, picks a real mic by name from the avfoundation list instead (`avfoundation_devices` now also returns a non-loopback mic index) — mirroring the guard Windows' `dshow_mic_device` already had. Decision affirmed: audio stays fully automatic for end users (Windows WASAPI loopback at task 16 = zero-setup game audio; mic follows system default with the loopback guard); an optional mic picker can join the real Settings screen (task 15); no output picker — capture should follow what the user hears.
- Task 14 — Stage 7 implementation (2026-07-04, awaiting live test): new `tray.rs` (tray icon from the bundle's window icon, tooltip, menu: Open Game Backlog / Quit; left-click opens, right-click menus, `show_main_window` helper that show+unminimize+focuses). Close-to-tray: `on_window_event` intercepts `CloseRequested` on `main`, prevents close, hides — tracker + hotkey keep running windowless; Quit goes tray → `app.exit(0)` → existing `RunEvent::Exit` → `clipper::stop`. macOS `RunEvent::Reopen` reopens from the Dock; the single-instance callback now `show`s (not just focuses) the window since it may be hidden. Launch-on-startup via `tauri-plugin-autostart` (LaunchAgent on macOS, registry on Windows) with `--hidden` args — setup hides the main window when that flag is present so login launches boot straight to the tray; exposed as `get/set_autostart_enabled` commands + a Settings "General" section toggle (state read from the OS, not the DB). Settings' switch markup extracted into a shared `Toggle` component.

- Task 15 close-out (2026-07-04): added `src/components/Search/Search.tsx` empty states — a "Search for a game" hint shown before any search, and a "No games found" card when a search returns zero results (`hasSearched` state gates which shows). Recommendations needed no changes, already had empty-state copy from task 12b. `tsc --noEmit` clean.
- Task 15 round 1 (2026-07-04, in progress): added `src/components/shared/ErrorBoundary.tsx` (class component, `getDerivedStateFromError`/`componentDidCatch`) and wrapped `<Outlet />` in `Layout/Shell.tsx` with it, keyed by `location.pathname` so a crash on one page shows a contained fallback card (message + Reload) instead of blanking the whole app, and navigating away auto-recovers. Upgraded Backlog's empty state (was a single sentence) to a dashed-border card with an icon and a "Search for a game" CTA linking to `/search`. Added a Dashboard first-run onboarding card — shown only when the backlog is entirely empty (no games playing/backlog/completed, no playtime, nothing currently playing) — with a welcome message and the same Search CTA, replacing the empty stat-grid/chart a brand-new install would otherwise see. Verified: `tsc --noEmit`, `npm run build`, `cargo check` all clean; live-tested in `npm run tauri dev` by navigating all six tabs and force-triggering the ErrorBoundary via a temporary `throw` injected into `Settings.tsx` (confirmed the fallback renders correctly and the sidebar stays interactive), then reverted the throw. The empty-state/onboarding cards themselves are implemented but not yet visually confirmed live by the user (needs an actual empty backlog to view).

- Task 15b close-out — screenshot-driven polish round (2026-07-04, approved live by the user): the user supplied screenshots of all 12 page states; one inline pass (no subagents) applied: (1) new `shared/EmptyState.tsx` — the single dashed empty-state card (icon tile, title, copy, optional CTA) now used by Backlog, Search (both pre-search and no-results), Discover For You, and Clips (which previously had only a bare sentence); (2) Dashboard first-run rebuilt as a vertically-centered welcome with three feature cards (auto tracking / Alt+F9 clips / Discover) — note its container needs `min-h-screen`, not `min-h-full`, because Shell's Dashboard wrapper div is heightless; empty 7-day chart and empty games-played shelf got dashed placeholders; (3) control-contrast pass: card "Add to Backlog" buttons got visible borders + a success-tinted "✓ Added" state (Search + Recommendations), Backlog action buttons brightened, exe-link input became a bordered transparent field, Settings toggles got a visible off state (ring + gray knob) and a CHALK on state (rust stays live-marker-only), TopNav now-playing name switched to chalk; (4) new `.kbd` chip utility in `index.css` for hotkeys (Clips + Settings copy); (5) chat: stronger user-bubble border, more transcript bottom padding; (6) Clips: card hover-lift + circular play chip, player modal fade/border/caption; (7) `tabbed_out.png` regenerated at 1920×1080 in Iron & Chalk via a scratchpad PIL script (SFNS.ttf Semibold variation — chalk tracked type, 44px rust bar, warm vignette); old clips keep the teal-era card baked into their footage, only new saves get the new card. `tsc`/`npm run build` clean; live-verified via the running dev app.

- Task 16 implementation (2026-07-05): release pipeline + the two Windows ship-blockers it gated. **`release.yml`**: `v*` tag → matrix build (windows-latest, macos-latest) → `tauri-apps/tauri-action@v0` → DRAFT GitHub Release with `.msi`/`.nsis`/`.dmg` (review assets, publish by hand). **ffmpeg sidecar**: `tauri.windows.conf.json` adds `bundle.externalBin: ["binaries/ffmpeg"]` (platform config auto-merges on Windows builds only, so macOS dev never needs the file); the workflow downloads gyan.dev's pinned `ffmpeg-8.0.1-essentials_build.zip` (versioned gyan URLs are permanent; BtbN "latest" assets rotate — the 7.1.1 URL guessed first was a live 404, verified 8.0.1 with curl before pinning) and stages `bin/ffmpeg.exe` as `src-tauri/binaries/ffmpeg-x86_64-pc-windows-msvc.exe` (gitignored); `clipper.rs::ffmpeg_path()` (OnceLock) resolves `ffmpeg[.exe]` next to `current_exe()` and falls back to PATH — all 7 ffmpeg spawn sites switched to it; `windows-check.yml` stages an empty placeholder sidecar so `cargo check` survives tauri-build's externalBin handling. **WASAPI loopback** (new `loopback.rs`, Windows-only module, `cpal` dep under `[target.'cfg(windows)']`): `default_output_spec()` reads the default output device's mix format (F32/I16 → `f32le`/`s16le`, else dshow fallback); `capture_input_args` prefers it over dshow loopback devices, adding a `-f f32le -ar .. -ac .. -i pipe:0` input; `spawn_ffmpeg` pipes stdin and hands it to `loopback::start`, which runs a detached thread: cpal input stream ON the output device (WASAPI loopback mode), callback → bounded channel (`try_send`, never blocks the audio thread) → writer loop that paces total bytes against the wall clock and pads with silence when the engine goes quiet (WASAPI delivers no packets during silence; without padding the audio track drifts against the video). Thread lifecycle is the pipe itself — capture killed → broken pipe → thread exits; nothing stored on `Capture`. Verified: local `cargo check` (mac) + `windows-check` CI (Windows, compiles cpal + loopback.rs + placeholder sidecar).

- Task 17 implementation (2026-07-06): Steam library import. **`steam.rs`** (new module): `resolve_steam_id` accepts a profile URL (`/profiles/<id64>` or `/id/<vanity>`), bare 17-digit steamID64, or bare vanity name — vanity resolved via `ISteamUser/ResolveVanityURL/v1`; `get_owned_games` hits `IPlayerService/GetOwnedGames/v1` (`include_appinfo`, `include_played_free_games`) — a private profile returns a bare `{"response":{}}` with 200 OK, detected and surfaced as a "set Game details to Public" error. `STEAM_API_KEY` is a bundled Rust const (same pattern as the RAWG key) — **left as an empty placeholder, user must fill it**. **Two-step flow**: `fetch_steam_library(profile)` resolves + fetches + sorts (most-played first, never-played alphabetical tail), flags games already in the backlog (by `steam_appid` or lowercased name), and remembers the input in `settings` (`steam_profile`, read back by `get_steam_profile` for prefill); `import_steam_games(picks)` enriches via RAWG in concurrent chunks of 8 (new shared `rawg::best_match` — exact-name-preference helper, tracker.rs's auto-add refactored onto it), takes the DB lock only between chunks (never across an await), and per game: skips if `steam_appid` already present, links `steam_appid` onto an existing row when the RAWG id or name already matches (hand-added or tracker-auto-added games), else inserts a new `backlog` row; emits `steam-import-progress {done,total}` per chunk + one `steam-import-done` at the end (Backlog listens and refreshes — deliberately one event, not per-game notices). New `games.steam_appid` column via `add_column_if_missing`. **UI**: Settings gains a "Steam" section (`SteamImport.tsx` — input + Fetch library button) opening a portal modal: filter box, select all/clear, checkbox list (already-in-backlog rows grayed with an "In backlog" tag), progress bar during import, summary panel (imported/linked/skipped) after; backdrop-click close is blocked mid-import. Verified: `cargo check`, `tsc --noEmit`, `npm run build` all clean.
- Task 17 close-out (2026-07-06): **live-verified by the user** — real profile fetched, library listed, import confirmed working in the dev app. Key handling finalized the same session: `STEAM_API_KEY` moved from a committed const to compile-time injection via `option_env!` — GitHub repo secret `STEAM_API_KEY` (set via `gh secret set`, wired into release.yml's tauri-action env for both matrix legs) + gitignored `src-tauri/.cargo/config.toml` `[env]` section for local dev (reverted to a placeholder after the test per user request; verified the key string exists nowhere in the working tree or git history). Getting a Steam key required the user to enable Steam Guard Mobile Authenticator first. Keyless fallbacks were investigated and are dead: steamcommunity's `games?xml=1` and games-page HTML endpoints both redirect to login now (verified with curl 2026-07-06) — a Web API key is the only path. Decision (user asked): the Steam key does NOT move to the Cloudflare Worker proxy — the unauthenticated worker URL is equally extractable from the binary, so proxying adds latency without protection; revisit only if the app distributes widely (then put Steam+RAWG behind the worker with a client check).

- Tasks 18+19 — Backlog → Library (2026-07-06, design + implementation in one session; **uncommitted**, user live click-through pending):
  - **Design round first** (per plan): 4 decisions via structured questions — status model = "mostly derived, light user marks" (activity derived from sessions; `completed` + `dropped`-rendered-as-"Not for me" are the only manual marks; wishlist a separate tab, not a status); browse surface = cover-art grid + full detail page; detail scope = playtime stats/trend + clips now, RAWG metadata behind an expander, session-history list cut; idle hero = recent activity ("Jump back in"). Then one localhost HTML mockup round (grid variants A/B, detail page, idle hero — served on :8845) approved with "looks great, proceed". Calls made on unanswered sub-questions: grid variant A (hover stats), not-for-me covers dimmed, detail as a real route.
  - **Data model**: no schema rebuild — `games.status` CHECK values reinterpreted (`backlog`=in-library, `dropped`="Not for me", `wishlist`=tab; `playing` legacy, normalized to `backlog` at every `db::init`). Tracker no longer writes status except wishlist→backlog on launch; auto-add inserts `backlog`.
  - **Backend**: new `get_library` (games + session aggregates in one query, replaced `get_backlog`+`get_playtime_totals` which were REMOVED), new `get_game_stats(id)` (tiles + 8 rolling-week sparkline buckets), `get_clips` gained optional `game_id` filter, `DashboardStats` fields → `games_in_library`/`games_played_count`.
  - **Frontend**: `Backlog/` deleted; new `Library/Library.tsx` (poster grid, tabs, search, chips, Select-based sort) + `Library/GameDetail.tsx` (hero, mark toggles, stat tiles, sparkline, clips strip reusing exported `ClipPlayer`, About expander, exe-link/remove utility row); routes `/library` + `/library/:id`; TopNav label "Library"; Dashboard idle hero = most recently played ("Jump back in" + last-played line, "Up next" gone), stat strip = In library/Played/Completed, first-run heuristic updated; store's unused `games` state removed; app-wide copy sweep backlog→library ("Add to Library" etc.).
  - **Shared `Select.tsx`** (user request, same session): custom dropdown for the whole app — native `<select>` popups can't be styled. Portal-rendered menu, keyboard nav, closes on outside click/scroll/resize/Escape, symmetric 120ms fade in/out (closing keeps it mounted through a reversed animation). The Library sort control uses it; all future dropdowns must (e.g. task 26's hotkey picker).
  - Verified: `cargo check` clean (no warnings), `tsc --noEmit` clean, `npm run build` clean, `npm run tauri dev` launched without panics.

## Blockers

_(none currently)_

## Open Questions

- release.yml has never actually run — the first `v*` tag push is the shakeout (tauri-action release creation, gyan.dev download, sidecar staging path, msi/nsis/dmg artifacts). Cut a `v0.1.0` tag when ready; the release lands as a draft so a broken run publishes nothing.
- The whole Windows runtime path is compile-verified only (`windows-check`): gdigrab capture, window-title scoping, WASAPI loopback audio (packet pacing/silence padding especially — real audio-engine behavior can't be simulated), dshow mic, tray/autostart/installer. Needs a session on real Windows hardware with the installed build.
- Game-audio-in-clips on the macOS dev machine: optional re-test whenever the user does the one-time Multi-Output Device setup (Audio MIDI Setup → speakers + BlackHole → select as output). Mic-in-clips no longer depends on that (loopback-as-default-input guard landed 2026-07-04, and the toggle just needs switching on). Windows ships zero-setup via WASAPI loopback at task 16.
- Post-ring-wrap saves (>3 min sessions) and window-cropped windowed-mode clips from round 2 haven't been explicitly confirmed by the user yet — piggyback on the next clip test.

## Decisions

- Marketing website will live in a **separate repo**, not this monorepo. This repo is app-only (+ future `proxy/` for the Cloudflare Worker). The site will pull the latest release info from the GitHub API rather than sharing code with this repo.
- Development happens on macOS; the real target is Windows. Windows-only pieces (`gdigrab` capture, registry-based launch-on-startup, `.msi` build) can't be tested locally on Mac — they'll be validated via the GitHub Actions Windows runner and, eventually, manual testing on real Windows hardware.
