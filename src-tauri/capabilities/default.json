{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the in-game overlay toast window",
  "windows": ["main", "clip-overlay"],
  "permissions": [
    "core:default",
    "opener:default",
    "global-shortcut:default",
    "notification:default"
  ]
}
